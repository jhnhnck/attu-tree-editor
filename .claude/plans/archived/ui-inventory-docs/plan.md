# plan: ui-inventory-docs

## phase 1 — move, refresh, and chart ui-inventory

**status:** closed — pending merge (878269c)

**scope:**
1. move `notes/reports/ui-inventory.md` → `notes/features/ui-inventory.md`
2. refresh the doc with `/reference-notes` (skip dock/windows/pills sections)
3. add mermaid charts at top of file: one for overall UI element structure, one per menu

**definition of done:** `notes/features/ui-inventory.md` exists, cross-references updated, mermaid charts render correctly, no regressions to other notes files.
