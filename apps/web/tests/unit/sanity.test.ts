/*
 * FamilyTreeEditor - phase 0 sanity check; replaced by real unit tests in phase 1
 * licensed under the MIT license; see LICENSE.md for full text
 */

import { describe, expect, it } from "vitest";

// placeholder so vitest has at least one passing case in phase 0
// real domain tests land in phase 1 alongside HaracalndeDate
describe("sanity", () => {
    it("runs", () => {
        expect(1 + 1).toBe(2);
    });
});
