# FamilyTreeEditor bug log

*known defects with reproducible misbehavior. feature work, deployment plumbing, and "this could be nicer" items live in [to-do.md](to-do.md). see the [meta](#meta) section at the end for format.*

---

## open

### family-view

- :o: `high priority` `medium effort` family-view card portraits never load even though the same person's portrait renders fine in the inspector panel - repro on the Akarian 2026-05 fixture: every with-portrait card shows an empty dark gradient slot on the canvas, but clicking the person opens the inspector with the portrait displayed correctly. so the portrait bytes / blob / data path is sound (inspector proves it) - the failure is specific to how the family-view card derives its `<img src>`. start by diffing the inspector portrait widget's source resolution against `FamilyViewCanvas.svelte` / its PersonNode card portrait slot: the inspector likely reads the resolved portrait through the same store the canvas should use, so the canvas is probably referencing a stale, missing, or never-populated url field (or rendering the slot before the async portrait resolves and never re-attempting). distinct from the canvas-section "portraits sometimes fail on reload" entry - this one is family-view-specific, deterministic (every card), and contrasts directly with a working inspector.
- :o: `medium priority` `low effort` `needs more info` family-view "Tokoat Kakay" (b. 31 TT) card renders ghost / placeholder-styled on the Akarian 2026-05 fixture - empty translucent body, no surname-colour accent stripe, faint outline - while every other card on the same rank (Jadotokur Dar, Hadagoi Lakar, Jatalantur Dar, Tanoat Kakay, Turaba Dakon, Othsachur Ikane) renders with a normal filled fill + accent stripe. Tokoat has a real birth-date field populated, so this is not a missing-person placeholder. *investigation 28 May 2026: confirmed PERSON_W=2 (card width 160px), CARD_H_WITH_PORTRAIT=2.4u=192px — portrait slot is 70%×160≈100px square, well inside the 192px card height, so portrait overflow is not the cause. toneClass is always sky/rose/amber (never empty), is-faded requires display=z0 (GEDCOM always sets z1), no ghost class is ever passed from FamilyViewCanvas. the private Akarian 2026-05 fixture is required to repro — cannot confirm root cause from the public codebase alone. likely candidates: (a) secondary-union placeSecondaryUnions assigns the card a rank that collides with an already-placed node causing zero-height or off-canvas placement, or (b) a fixture-specific data shape (cross-generation union? duplicate couple record?) hits a silent layout path that produces an empty node. root cause unconfirmed; fixture access needed.*
- :o: `high priority` `medium effort` `needs more info` family-view "also show {partner} alongside" union-picker action does nothing - picker menu in `FamilyViewCanvas.svelte` (around line 1578) calls `secondaryUnionState.expand()` via `onPickerShowAlongside()` (around line 911); gated by `fte.layout.familyViewSecondaryUnion` flag (default on). picker opens but canvas doesn't change. likely either the expand store isn't reaching `planRank`, or the secondary-slot emission silently drops for the fixture shape. *no-repro on 2026-05-24 parallel-orchestrate trace: existing e2e (`visual-secondary-union.spec`) passes, all 16 secondaryUnion unit tests pass, multi-union fixture shows the secondary partner + their kids on the canvas after clicking "also show alongside". needs concrete reporter steps (fixture, exact card clicked, browser, localStorage state of `fte.layout.familyViewSecondaryUnion`). adjacent finding (now fixed 28 May 2026): `recomputeAfterCollapse` in `engines/family-view/layout.ts` did not forward `expandedSecondaryUnions` to `selectBoundedSubset`, so a secondary partner could vanish on the auto-collapse pass when visible count exceeds 50 — fixed in the item-tokoat-ghost-card worktree.*
- :o: `high priority` `medium effort` `needs more info` family-view selection→focus path highlight does not paint - the `--fte-on-path-stroke-width` / `--fte-on-path-ring-color` tokens + `.family-view-onpath-edge` selector shipped 23 May 2026 (see fixed section) but on current trunk the highlight is invisible. likely a regression in the family-view edge classname pipeline or a specificity loss. verify View > Overlays > Path highlight is on, then inspect computed styles on a selected card's edges. *no-repro on 2026-05-24 parallel-orchestrate trace: tokens present + selector emitted + computed styles applied (`stroke-width: 2.5px`, drop-shadow + card ring active) on both `multi-union.ged` and `dense-tree` fixtures in dark + light themes; screenshot matches the existing `visual-path-highlight` golden. plausible reading: the 23 May intensity reduction (stroke 5→2.5, ring opacity 0.7→0.35) reads as too subtle on some displays — that would be a perception/tuning ticket, not a paint regression. needs reporter to confirm whether they meant "literally invisible" or "too subtle to notice", plus their display + theme.*
- :o: `low priority` `low effort` family-view debug `showSecondaryUnionState` pill is svg text rendered just below each card; on dense rows it may visually overlap the next rank's connectors when both the pill and the connector are visible. toggle-gated (off by default) so no production impact; revisit if it ever becomes default-on. surfaced during family-view-debug plan, phase 1.
- :o: `low priority` `low effort` family-view debug `showLastEditHalo` yellow stroke (`hsl(50 95% 55%)`) achieves only ~2:1 contrast against the light-theme canvas vs ~3:1+ for the other phase-5 strokes - matches the layered overlay's `#fde047` parity (same low-contrast issue there), but worth flagging if a user complains the halo is hard to spot in the light theme. surfaced during family-view-debug plan, phase 5.

### tree-view (layered engine)

*all items below are layered-engine specific; demoted to lower priority by the family-view rollout.*

- :o: `medium priority` `medium effort` residual ghost↔near x-spread (3 ghosts, up to ~10.4u) after the order-level adjacency fix - on the Akaria DEMO fixture, three ghosts still sit further from their near than the canonical DELTA (2.5u) even though the order pass leaves no foreign node between them. Two have nothing between near and ghost; one has only a sibling ghost between (a 5u = 2×DELTA spread for the far member of a 2-ghost cluster, which is geometrically correct). Root cause is in `passes/place.ts`: `enforceSeparation` is a minimum-gap pass, so it cannot pull a pair closer than B-K averaging + `centerUnderParents` placed them. The closePairs gap of DELTA is registered correctly; what's missing is a "snap-closer" step for registered close pairs that ended up too far apart after B-K. Distinct from the order-level bug fixed `26 May 2026` (which eliminated foreign-node interleaving) and from the multi-spouse bond crossing fix also `26 May 2026` (which is a router issue). Open offenders on DEMO: `ghost:S6QCU|VBPOA` (10.375u), `ghost:CFNUE|8VTYR` (3.85u), `ghost:ZCOQF|T46RT` (5.0u, the geometrically-correct 2×DELTA case)
- :o: `high priority` `medium effort` zoom UI still broken (user-reconfirmed 24 May 2026, post the 23-24 May zoom fix wave: semantic-100% landing, anchor-aware `setScale` across all 3 engine canvases, `computeFit` chrome-inset math + vertical centring). still no concrete repro. candidates to chase: zoom anchor mismatches (wheel vs. button vs. slider vs. exact-percent path), the +/- widget step size, the slider mapping, fit-to-window after the chrome-inset change, or the semantic-100% reference itself drifting from user expectation. needs hands-on bisection in each of TreeCanvas / FamilyViewCanvas / HyperbolicCanvas with the specific knob and engine noted.
- :o: `low priority` `low effort` cross-rank couple orientation remains order-driven after the father-left tie-break in `passes/order.ts`. The tie-break is 100% effective on same-rank both-known couples (37/37 on the Akarians DEMO fixture) but cannot reach cross-rank pairs because their real-person X positions are decided by independent per-rank ordering - `repairCoupleAdjacency` only operates within a rank. **Partial close (commit 7f12a72)**: the couple-equalisation post-pass collapses cross-rank cases where one partner is unparented to same-rank (the tie-break then applies). Genuinely-different-generation couples (both partners parented at different depths) remain order-driven. On Akarians the overall both-known male-left ratio sits at ~57% (100% on same-rank + ~50% on cross-rank). The both-parented residual stays open.

### inspector

- :o: `high priority` `medium effort` `needs more info` changing inspector tabs sometimes freezes the entire UI until reload - user-reported intermittent: clicking between Personal / Connections / Bonds / Groups / Sibship / Debug tabs in the inspector occasionally locks the page (no canvas interaction, no menu open, no keyboard response) and only a hard reload recovers. no concrete repro yet. suspects: (a) an infinite re-render loop from a `$effect` in one of the tab components reacting to a derived store its own mount writes back to, (b) a synchronous heavy computation kicked off on tab-mount that blocks the main thread for several seconds (Connections tab walks the relationship graph; on dense fixtures this is the most expensive tab), (c) a leaked event handler / observer from the previous tab's `onDestroy` path that double-fires on the next mount. needs reporter steps: which fixture, which tab transition (A→B), how often (1-in-N clicks), browser + devtools performance trace covering the freeze window. when reproed, check the Connections tab's path-walk for memoisation gaps and any `$effect` reading + writing the same store.
- :o: `high priority` `medium effort` `needs more info` editing a person's gender silently drops them from their children's `parentIds` - user-reported data-loss bug. `updatePerson()` in `lib/domain/tree.ts:150` is a field-level patch with no `parentIds` mutation, so the disconnect can't be in the domain layer. suspected: a `PersonPatch` derivation somewhere in `PersonalTab.svelte` commit chain that re-infers couple/parent assignments from gender. needs urgent repro and root-cause trace; treat as data-loss class. *no-repro on 2026-05-24 parallel-orchestrate trace: the full commit chain (`PersonalTab.commitIdentity → Inspector.onpatch → App.onSave → treeStore.update → updatePerson`) is verified gender-only end-to-end; 3 repro tests against the exact scenario all pass (parent's gender changes, child's parentIds intact). `linkParent()` does derive role from gender but only on explicit link, not on gender-patch. plausible alternate causes per slicer: (a) older revision since fixed, (b) GEDCOM round-trip stripping the link, (c) FastAPI sync-path overwrite, (d) reporter saw a different bug. needs concrete repro steps from reporter (browser, fixture, step-by-step, exported .gdz before+after, whether sync is in play).*

### canvas

- :o: `medium priority` `low effort` (phase 5 ui-invariant-tests) hyperbolic engine emits duplicate keyed-each entries on Inbred Family fixture - HyperbolicCanvas mounts then svelte's keyed-each emits `each_key_duplicate` for `bond:IC6CP|WIF9X:0` at indexes 4 and 5. emission lives in `lib/layout/engines/hyperbolic-lr/layout.ts:455-473` where the loop walks `tree.couples` and builds `bond:<sortedKey>:<unionIndex>` - the duplicate suggests two CoupleRecord entries with the same (sortedIds, unionIndex). repro: mount `HyperbolicCanvas` against `notes/examples/Inbred Family.gdz` under jsdom (phase-1b shims active). likely either a missing dedupe pass in `emitCoupleEdges` or two CoupleRecord entries with colliding `unionIndex` from the source GDZ. surfaced during parity-matrix phase 5; mitigated locally by switching the hyperbolic cell fixture to `eightPersonFamily()`.
- :o: `high priority` `medium effort` `needs more info` portrait images sometimes fail to appear on reload - user-reported intermittent: after a page reload, some person cards render with their portrait slot empty (or showing the no-portrait placeholder) even though the image existed pre-reload. no concrete repro yet - may correlate with cache state, blob/object-URL lifecycle across reloads, or async hydration ordering (cards mount before image bytes resolve and never re-attempt). likely places to check: `PersonNode.svelte` portrait `<img src>` derivation (is the URL a stable per-load value or a stale objectURL from a prior session persisted into the tree?); the portrait-blob fetch path (sqlite-backed portraits in `data/` vs in-tree base64); any `URL.createObjectURL` calls that aren't revoked + recreated on hydrate. needs reporter steps: which fixture, how many cards affected, whether the missing portraits are the same set across reloads or random, and a network-tab capture of the failing requests.
- :o: `low priority` `low effort` TreeCanvas (layered) and HyperbolicCanvas edges may read too thin at low zoom for the same reason the family-view ones did (24 May 2026 fix) - both engines emit `<path stroke-width="1" vector-effect="non-scaling-stroke">` (TreeCanvas.svelte:1196-1198, HyperbolicCanvas.svelte:553-556). not user-reported yet; verify by panning out to ~25-50% on a dense fixture in each engine, and if thin, mirror the family-view bump (`stroke-1` to `stroke-[1.5]` equivalent). would re-baseline the layered + hyperbolic visual goldens.
- :o: `low priority` `low effort` pill drag-to-reorder can collapse a *different* open panel mid-drag - dragging a dock pill fires App.svelte's outside-click-close listener for any other expanded panel, collapsing it. arguably correct (click-elsewhere closes a popover) but a sharp edge; if it ever feels wrong, exempt an in-progress pill-reorder gesture from the outside-click-close path.

### shell

- :o: `low priority` `low effort` `needs more info` "fit selection" (Shift+1, `view.fitSelection`) may be redundant given single-select - the command zooms so the single selected card fills the viewport (`TreeCanvas.svelte:667-683`, mirrored in `FamilyViewCanvas.svelte:883` and `HyperbolicCanvas.svelte:456`), which is a different output from "fit to view" (entire-tree bbox) and "center on selection" (pan only, no scale change), but in practice "one card filling the viewport" is an extreme zoom with low utility - centering already brings the card on-screen and most users want context, not a card-fills-screen view. multi-select doesn't exist (`selection.select()` is single-id), so the original "fit a multi-card bbox" use-case never lands. verify by trying all 3 (fit to view / center / fit selection) on a representative fixture; if fit-selection is just "zoom way in on one card", drop the action + Shift+1 binding + command-palette entry rather than keeping a near-useless third zoom verb. revisit if/when multi-select ships - then fit-selection-bbox becomes meaningful.
- :o: `medium priority` `low effort` title bar + menu bar do not render above all other UI - the top chrome (title strip + menu bar wrappers in `Shell.svelte`) is not the highest stacking layer over the canvas — cards transformed via `translate3d` create their own stacking contexts that can paint over the strip, and tall toasts / overlay panels can obscure it. expected: the top chrome is the highest stacking layer on the page, beneath only an explicit blocking modal. fix: hoist the shell title strip + menu bar to a z-index above the canvas/card layers and audit any descendant `translate3d` / `transform` that creates a stacking context inside.

### portrait cropper

- :o: `high priority` `low effort` portrait cropper mobile interface unverified - ios safari `<canvas>` inside a native `<dialog>` may not deliver two distinct `pointerId`s for two-finger pinch-zoom; probe scaffolded at `apps/web/public/probes/touch.html` but never run on real hardware. paired exif-orientation probe at `apps/web/public/probes/exif.html` is also unrun. cropperjs (which handled mobile) is already removed; if the multi-touch probe returns "no", recovery is `git revert d3693a9 && pnpm install` to restore the legacy dialog. probes themselves are ~1 afternoon on a real iphone.

### wiki-editor

- :o: `low priority` `low effort` `lang-wikitext` external-link opening bracket `[` gets the URL token color instead of the muted bracket color (`extLinkBracket` is only emitted for the closing `]`) - inconsistent with how wikilink brackets are styled. fix alongside the image/file link token design revisit. 🎯 carried in from plan: wiki-editor-syntax (B01)
- :o: `low priority` `low effort` `lang-wikitext` table captions (`tableCap`) render in monospace - mapped to `tags.meta`, which inherits the `pre` rule's `fontFamily: var(--font-mono)`. fix by splitting `pre` onto a custom tag or giving `tableCap` its own tag. 🎯 carried in from plan: wiki-editor-syntax (B08)
- :o: `medium priority` `medium effort` `lang-wikitext` `inTable` parser state is never reset in `blankLine()` - a missed `|}` close leaves `state.inTable = true` for the rest of the document, so any later stray `|` gets highlighted as a table separator. degrades gracefully (dim, non-destructive misstyling) but is a real state leak. 🎯 carried in from plan: wiki-editor-syntax (B10)
- :o: `low priority` `low effort` wiki-editor theme.css `--color-syn-struct` measures ~2.7:1 contrast against the dark editor background (target ~3:1+ to match the other syntax tokens) - may need a bump from `#56504a` to roughly `#70685e`. visual verification of the full Melange palette in a running browser was also never completed before merge. 🎯 carried in from plan: wiki-editor-theme

---

## fixed

### family-view

- :red_circle: `29 May 2026` family-view path-highlight no longer spills into a spouse's other unions - `bond:` edges in `edgeOnPath()` now require both endpoints on-path (`every` instead of `some`), stopping the one-hop overshoot where selecting a multi-union spouse lit up their other bonds. closed in c3b7d27.
- :red_circle: `29 May 2026` family-view with-portrait cards over-tall - `CARD_H_WITH_PORTRAIT` was `CARD_H * 2 = 2.4u = 192px`, calibrated for the old 3:4 portrait slot; the 28 May portrait-slot fix (3:4 → 1:1) left a ~46px dead gap at the bottom of every with-portrait card. fix: changed the constant to the literal `2.0u = 160px` in `engines/family-view/layout.ts`. closed in 2bd484f.
- :red_circle: `28 May 2026` family-view couple-bond endpoint poked a few pixels past the card edge - repro on `notes/examples/Inbred Family.gdz` between Graps Nokar and Banchar Nokar: the pink bond line terminated at `CARD_VISIBLE_INSET_U = 3/80 u` (3 px CSS) inside the card's outer rect, but the card border is only 2 px wide, so the endpoint sat 1 px past the inner border face, visibly intruding into the card at high zoom. fix: reduced `CARD_VISIBLE_INSET_U` from `3/80` to `1/80` u (= half the 2 px border stroke) in `engines/family-view/layout.ts:coupleConnector`, so the bond now terminates at the center of the stroked border. the old value was inherited from the selection-ring shadow spread (3 px inset box-shadow), which is the wrong reference for the bond endpoint.
- :red_circle: `28 May 2026` family-view root card carried a persistent outer ring that doubled up with the selection ring - root cause: `cardOnPath()` in `FamilyViewCanvas.svelte` included the `activeFocus` card in `family-view-onpath` because the BFS path between any selected card and the focus always includes the focus as its endpoint. result: root (the default focus) always got the `family-view-onpath` box-shadow ring whenever any card was selected, and when root was itself selected both the yellow inset selection ring and the blue outer path ring appeared simultaneously. fix: added a guard in `cardOnPath()` that returns false for the `activeFocus` card - it is the path endpoint and the layout anchor, not an intermediate on-path card. the selection ring is now the only stroke indicator on the root card.
- :red_circle: `28 May 2026` family-view `onPickerSelect` (primary-union swap) did not set `suppressNextFit` — latent: the `auto-fit-suppression-handlers.test.ts` row `primary-union-swap` passed because `multi-union.ged`'s F1/F2 are shape-symmetric, so the bbox was invariant across the swap; an asymmetric fixture would expose the same zoom/pan yank as the show-alongside bug (fixed 27 May 2026). fix: added `suppressNextFit = true` as the first line of `onPickerSelect` in `FamilyViewCanvas.svelte`, matching the pattern used by `onPickerShowAlongside` / `onPickerHideAlongside`. carried in from plan: ui-invariant-tests (2026-05-27).
- :red_circle: `28 May 2026` family-view fit-effect key omitted `hostW`/`hostH` — a host resize that left the layout bbox unchanged never triggered a refit (e.g. dock panel open/close stretching canvas width while the tree stayed the same). fix: appended `:${hostW}:${hostH}` to the fit-key string in `FamilyViewCanvas.svelte` so any host dimension change fires the auto-fit `$effect`. `it.fails` row `host resize` in `tests/component/auto-fit-suppression-handlers.test.ts` promoted to plain `it`. carried in from plan: ui-invariant-tests (2026-05-27).
- :red_circle: `28 May 2026` path highlight did not include the selected person's children - the selection→focus highlight covered ancestor edges but stopped at the selected card; stub edges (selected → child) were never lit because edgeOnPath gates stubs on the child being in the pathSet, but bfsPath only returned the path to the focus ancestor. fix in `engines/family-view/path.ts:usePath`: after building the BFS path set, iterate all persons and add any whose parentIds includes the selected person - one hop downward, mirroring the childrenOf loop in bfsPath. stem/bus/bond edges that already include the selected parent continue to light up via any-implicated-on-path. three new unit tests pin the child-inclusion, multi-child, and one-hop-only behaviours.
- :red_circle: `28 May 2026` PersonNode portrait slot was 3:4 but source portraits are 1:1 (600x600) - `object-cover object-top` silently bottom-cropped every portrait; the lower third of each image never showed. fix: changed `aspect-ratio` in `.portrait-slot` from `3 / 4` to `1 / 1` and removed the `object-top` bias so the full square image is visible. supersedes the misframed close in [reports/completed-items-report.md](reports/completed-items-report.md#bug-24-personnode-portrait-object-fit).
- :red_circle: `27 May 2026` family-view "show alongside" / "hide alongside" secondary-union picker actions also auto-refit the viewport - same shape as the `+` expand-subtree refit bug (fixed 24 May 2026): `onPickerShowAlongside` / `onPickerHideAlongside` in `FamilyViewCanvas.svelte` mutate `secondaryRev`, the layout bbox changes, the fit-key `$effect` runs and yanks zoom/pan. fix: added `suppressNextFit = true` at the top of both handlers, matching the pattern already used by the expand handlers. `it.fails` rows `show-alongside`, `hide-alongside`, `secondary-union collapse` in `tests/component/auto-fit-suppression-handlers.test.ts` promoted to plain `it`.
- :red_circle: `26 May 2026` family-view couple/marriage bonds routed straight through unrelated same-rank cards - on the Akarian 2026-05 fixture, 137 violations across all foci where a 2-couple bond's single horizontal segment at the row midline visibly crossed one or more intervening cards. Root cause: `coupleConnector` in `engines/family-view/layout.ts` emitted a two-point line from left-partner right-edge to right-partner left-edge without obstacle awareness, but `emitAnchorsAndEdges` walks `tree.couples` for every union regardless of whether `planRank` packed the two partners into the same slot - when each partner ended up in a different couple slot (third+ union, polygamy chain), unrelated cards landed between them on the same rank. Fix: introduced `cardsByRankFrom` index + `detourBondAroundCards` helper that emits a multi-point polyline hopping up into the gutter above the parent row (at `topY - BOND_DETOUR_CLEAR_U`) around each blocking AABB, mirroring the layered engine's `detourAroundCards` shape in `passes/route.ts`. Adjacent couples (the overwhelming majority) still get a clean 2-point segment. Akarians invariant pinned in new `tests/unit/engines/family-view/bond-detour.test.ts`; sibling sanity test in `engine.test.ts` asserts adjacent couples stay 2-point.
- :red_circle: `26 May 2026` family-view dropped the bond on a cross-generation couple (uncle/niece) and routed the shared child through the multi-parent pill - repro on `notes/examples/Inbred Family.gdz` with focus = New(I6): the Unclek (gen 2) + Neicea (gen 3) couple's two partners landed on different ranks, hitting the `aNode.rank !== bNode.rank` silent `continue` in `emitAnchorsAndEdges` so the couple emitted no anchor and no bond. New(I7) then fell through to the multi-parent fallback and rendered as two `drop:union:multi:...` segments converging on a pill in the inter-rank gutter, with no visible marker that Unclek and Neicea were married. fix in `engines/family-view/layout.ts`: replaced the `continue` with a new `emitCrossRankCouple` helper that emits an L-shaped slack bond through the gutter (upper partner bottom-mid → gutter midY → lower partner top-mid, role=married) and roots the sibship on the lower partner via the same solo-style stem+bus+stubs shape used elsewhere, marking shared kids as `childCovered` so the multi-parent path skips them. ranks are unaffected — the reporter's "rank-collapse" framing was the visual effect of the missing couple bond, not an actual rank shuffle. unit test in `tests/unit/engines/family-view/engine.test.ts` pins the new shape on the inbred fixture.
- :red_circle: `26 May 2026` family-view placed a single-parent sibship under the wrong adjacent couple - on the Akarian 2026-05 fixture, Jalaman Dar's kids (single parent, no co-parent shown) blended into the sibship of his married brother Thradach Dar + Radanagoi Lakar because every single-parent child got its own per-child L-drop with no shared bus, so three kids' horizontal segments at the same y looked like one continuous bus extending the neighbouring couple's. fix in `engines/family-view/layout.ts:emitAnchorsAndEdges`: replaced the per-child solo-anchor loop with a per-(parent, child-rank) bundling pass that mirrors the 2-couple geometry - one anchor + stem-down-from-parent-midX + shared bus + per-child stubs. side benefit: the bus now centers the visible sibship under the lone parent's column instead of letting kids float on the row's slot-order. single-child single-parent cases stay clean (bus collapses to a zero-length segment at the shared midX). unit test in `tests/unit/engines/family-view/engine.test.ts` pins the new shape.
- :red_circle: `26 May 2026` family-view path-highlight contradicted itself across siblings in the same visual row - paired with the placement bug above: selecting Hadagoi Lakar (child of Thradach+Radanagoi) lit a different parent-edge path than her visual row-mate Jatalantur Dar (child of Jalaman, single). highlight was always correct; the placement bug above made the visual contradiction read as a highlight bug. resolved by the placement fix - no edge-classification change needed.
- :red_circle: `26 May 2026` family-view selecting an off-subset person left the canvas blank for them - the family-view `onselect` handler in `App.svelte` only wrote to the selection store; for ids outside the current subset (non-primary partner, rank-cutoff, auto-collapsed) the canvas kept the previous focus's subset rendered. fix: when the new selection is missing from `familyViewSubset.visible`, call `canvasController.centerOnPerson(id)` so `recenterOn` falls through to `focusOverride = id` and the family-view engine rebuilds the subset around the new person. uses the id-taking controller method instead of `focusSelection()` to avoid the stale-prop race on the same tick.
- :red_circle: `24 May 2026` family-view edges too thin to see at low zoom - the off-path edges, group frames, sibship brackets / tie-bars, and overlay (sworn / transformation / alias / severance) lines all sat at 1.0-2.25px while `vector-effect="non-scaling-stroke"` kept the nominal width fixed in screen-px; once cards shrank at ~25-50% zoom the lines read as too thin. fix: bumped the off-path edge stroke from `stroke-1` to `stroke-[1.5]` in `FamilyViewCanvas.svelte` `edgeClass()` and bumped the css buckets (`.family-view-group`, `.family-view-sibship*`, `.family-view-overlay-*`) by +0.5-0.75px in `app.css`; on-path edges kept the existing `--fte-on-path-stroke-width` (2.5).
- :red_circle: `24 May 2026` debug pill was hidden under the family-view engine - the bottom-left bottom-bar wrapper rendered only when `layoutStats` was truthy *or* the user hadn't manually hidden the pill; the stats pill itself rendered unconditionally on any engine that emitted layoutStats. fix in `App.svelte`: gate the stats pill on `selectedEngine === "layered"` (no cluster analogue under family-view / hyperbolic) and keep the debug pill engine-agnostic via the `!debugPillHidden` branch, so family-view users now get the Ctrl+Shift+D entry point.
- :red_circle: `23 May 2026` smooth-diff animation absent - CSS-transition-on-transform (250ms cubic-bezier) on card + badge wrapper divs; `fte.overlays.smoothDiff` localStorage flag default-on; `.family-view-smooth-card` class + `data-smooth-diff="true"` attribute drive declarative wiring; global `prefers-reduced-motion: reduce` zeroes the transition. SVG edges + mount/unmount jump-cut as known scope-bounds (see to-do.md for the completeness follow-ups).

### tree-view (layered engine)

- :red_circle: `27 May 2026` tree-view (layered) selection highlight paints every ghost duplicate of the selected person, not just the canonical card - on reload (or any other path that resolves `selectedPersonId` after layout) all rank-duplicates of that id render with the selection ring. fix in `TreeCanvas.svelte` `isSelected` derivation: added `!v.isGhost` guard to the fallback branch so the `selected` class is gated on the canonical (non-ghost) instance when no `selectedInstanceKey` is set.
- :red_circle: `26 May 2026` multi-spouse bond passes through intervening ghost card - the horizontal bond is already detoured by `detourAroundCards`, but the joint-couple parent-drop hung from the bond's geometric midpoint, which lands inside an intervening co-spouse ghost's AABB on the parents' rank (the Kadar Arkaran case: drop at x=57.4 inside Harmain ghost x∈[56.4, 58.4]). Verticals are not detoured by design, so the anchor x must shift at emission time. Fix in `passes/route.ts`: build a `cardsByRank` index up front, then a `safeAnchorX` helper shifts the joint-couple parent-drop anchor sideways to clear any unrelated same-rank card while staying inside the bond's [rightX(l), leftX(r)] extent, preferring the side closer to the children's centroid. Bus afterwards is horizontal so `detourAroundCards` takes over. Two new unit tests in `tests/unit/layout/route.test.ts` pin the shift (synthetic Kadar-shaped fixture: real partner + two same-rank co-spouse ghosts).
- :red_circle: `26 May 2026` ghost-near adjacency miss for ~5% of ghosts on multi-spouse near persons - `passes/order.ts` gained a `repairGhostClusterAdjacency` post-pass that runs after `repairCoupleAdjacency` in the final cleanup. When a near has both a same-rank spouse and one or more ghosts, the couple-adjacency repair pulled the spouse adjacent to the near and displaced the ghosts; cluster-block repair ran earlier in the loop so its work was overwritten. The new pass re-seats every ghost cluster contiguous to its near, placing the ghosts on the opposite side of the same-rank spouse so couple adjacency is preserved. Result on the Akaria DEMO fixture (1802 people, 167 ghosts): zero ghosts with a foreign node interleaved between them and their near. Residual x-spread on 3 ghosts traced to B-K averaging / `centerUnderParents` and tracked as a separate open item; the order-level adjacency invariant the bug log called out is now met. Regression covered by `tests/unit/layout/order.test.ts` (3 new unit cases for same-rank-spouse + ghost(s)) and `tests/unit/layout/phase4-dod.test.ts` (Akarians-fixture invariant: no foreign node between ghost and its near in the order)
- :red_circle: `14 May 2026` same-rank short couple bond renders as two stub segments on small fixtures - `route.ts:296` `maxBondSpan = min(MAX_BOND_SPAN_CEILING, bbox.width/4)` collapsed to ~3.5u on tiny trees (8-person fixture, bbox 7.9u wide), so any couple with children pulling bondSpan past that became a `/stub-l` + `/stub-r` pair. Fix: floor the threshold at `BUNDLE_THRESHOLD` (= 8 × `ROW_H`). Effect: same-rank short-bond stub pairs dropped 1→0 / 2→0 / 3→1 across `eightPersonFamily`, `ghostStrandingDistilled`, and Akarians; Akarians' remaining stub is a legitimate cross-cluster long-bond
- :red_circle: `14 May 2026` same-rank both-known couple orientation was order-driven (50/50 male-left vs female-left) - `passes/order.ts` `repairCoupleAdjacency` gained a `preferredLeft` map (built from `tree` when threaded by `LayeredEngine`) that picks the male partner as anchor for mixed-gender both-known couples. Same-sex couples, unknown-gender pairs, and multi-spouse secondary unions fall through to position-based ordering. Matches the hyperbolic engine convention from commit `e3e7d8c`. Result: 100% male-left on all 37 same-rank both-known couples on the Akarians DEMO fixture. Cross-rank orientation remains a separate open item per the entry above
- :red_circle: `14 May 2026` ~12% of drops had negative height - root cause: `passes/layer.ts`'s `computeRanks` longest-path BFS ignored spouse edges, so a couple with one parented + one unparented partner ended up on different ranks. The joint child then took max(parent ranks) + 1, which could be above the unparented partner's rank, producing a negative-height drop. Fixed by a couple-equalisation post-pass in `layer.ts` that raises unparented partners to match the parented partner's rank. Commit 7f12a72.
- :red_circle: `14 May 2026` spurious ghost on the top rank after "add parent" when the added-to person's spouse has no parents - same root cause as the negative-drop bug above (computeRanks parent-DAG-only). Fixed by the same `layer.ts` couple-equalisation post-pass that closes the negative-drop. Commit 7f12a72.

### inspector

- :red_circle: `26 May 2026` inspector panel grew document height instead of scrolling internally (regression of 23 May 2026 fix `7de1c41`) - the 23 May fix added `min-h-0 overflow-hidden` to `<aside>` and `min-h-0` to the tab body, but the regression rode in via the App-level flex chain: `<main>` was `relative flex flex-1 overflow-clip` with no `min-h-0`, so as a `flex-1` child of the `h-dvh flex-col` shell it inherited `min-height: auto` and grew with its tallest descendant once the aside's content exceeded the viewport. fix: add `min-h-0` to `<main>` in `App.svelte` so the flex item can shrink below its content height, letting the aside's own min-h-0 + overflow-hidden engage and the inner tab body's overflow-y-auto take over. supersedes the inspector-scroll regression entry.
- :red_circle: `24 May 2026` `inspector-more-actions-smoke.spec.ts:59` asserted `getByText("root updated")` but the post-24 May 2026 toast copy is `"${name} is now the tree root"` - the wave-1 toast-z-index fix (`f88b7c4`) rewrote the assertion to `getByText(/is now the tree root/i)` so chromium + mobile e2e now match the actual toast. carried in from the import-wizard-and-family-echo plan; resolved as a side effect of the toast-intercepts-menu slice.
- :red_circle: `24 May 2026` Connections-tab "trace path to…" action set `traceTargetId` but no canvas highlight painted - removed the action; the in-flight "selectable lineage trace" feature in to-do.md is the canonical replacement and selection→focus path highlight already ships. cleaned up the full prop chain (`traceTargetId` / `onsetTraceTarget` from `ConnectionsTab.svelte` + `Inspector.svelte`, `traceIds` / `tracePath` plumbing in `App.svelte` + `TreeCanvas.svelte` + `DebugOverlay.svelte` + `debugTypes.ts`). `__treeDebug.findPath` console helper kept; `pathHighlight.ts` `bundlesForPath` util kept as a building block for the upcoming feature.
- :red_circle: `23 May 2026` inspector panel grew the document height instead of scrolling internally - `<aside aria-label="person inspector">` gained `min-h-0 overflow-hidden` so it stays bounded by the flex row, and the tab body gained `min-h-0` so `flex-1 overflow-y-auto` actually engages. empty-state summary panel got the same min-h-0 fix.
- :red_circle: `23 May 2026` inspector panel content couldn't scroll - same fix as the document-height bug above. flex children need `min-height: 0` to allow `overflow-y: auto` to clip their content; without it the panel grows past its parent and neither wheel nor drag intercepts a scrollable surface.

### canvas

- :red_circle: `29 May 2026` `canvas-chrome-dock.spec.ts` test `h)` pinned pill height ≤ 29px (1.75rem × 16px) but root font-size is 110% since commit `abea73c`, so 1.75rem = 30.8px > 29 and the test failed. resolved by the canvas-window-manager migration: test `h)` was rewritten as "layout-metrics Window titlebar stays visible when body collapses" and now asserts a `>= 28 - 1` LOWER bound on the titlebar `min-height` contract instead of a `<= 29` upper bound, so the 110% root no longer trips it. 🎯 carried in from plan: canvas-window-manager; closed under canvas-chrome-v2 (2026-05-29).
- :red_circle: `28 May 2026` tree view auto-centres / refits even when the full tree already fits comfortably on screen - any bbox-change tick (subtree expand, host resize after dock change) reruns the auto-fit `$effect` in `TreeCanvas.svelte` and `FamilyViewCanvas.svelte`, yanking zoom + pan even when the content is already fully visible. fix: added a passive `contentFitsInView()` predicate in both canvases that computes the content bbox in screen-px (`contentLeft = panX`, `contentRight = canvasW * scale + panX`, etc.) against the chrome-aware visible band (`insets.left .. hostW - insets.right`, `insets.top .. hostH - insets.bottom`) with a 60px margin. called inside `untrack()` immediately before the `fitToView()` / `resetView()` call; returns early if the content already fits. does not interact with `suppressNextFit` (the explicit user-action override).
- :red_circle: `28 May 2026` click-and-drag did not pan the canvas when the pointer-down landed on a card — `FamilyViewCanvas.svelte`'s `onPointerDown` returned early for any `[data-person-id]` target, so `dragStart` was never set and `onPointerMove` was a no-op from card body. fix: removed the `[data-person-id]` early-return; added `onCard` flag to `dragStart`; `onPointerUp` now suppresses the post-drag click on cards (matching `TreeCanvas`'s window-listener approach) and only calls `ondeselect()` when the no-drag tap started on the canvas background — card-body taps let the card's own `onclick` fire. explicit interactive controls (`[data-expand-toggle]`, `[data-badge-id]`, `[data-union-picker]`, `[data-add-toggle]`) keep their existing early-return guards. `TreeCanvas` already set `dragState` unconditionally and was unaffected.
- :red_circle: `28 May 2026` window resize did not translate the existing viewport center onto the new viewport center - on a browser resize (or inspector dock open/close), `pan` was kept verbatim against the old host rect, so content drifted off-screen. fix: in both `TreeCanvas.svelte` and `FamilyViewCanvas.svelte` the `ResizeObserver` callback now applies a pan correction before updating `hostW`/`hostH`: `panX += (newW - hostW) / (2 * scale)`, same for Y. dividing by `scale` converts the screen-space half-delta to canvas units so the world-space point that was under the old viewport center stays under the new one. correction fires before the auto-fit gate so if auto-fit runs it centres on the correct anchor. follow-up to bug-23 in [reports/completed-items-report.md](reports/completed-items-report.md#bug-23-auto-fit-not-vertically-centred).
- :red_circle: `28 May 2026` surviving family-view e2e specs queried `getByRole("region", { name: /family view canvas/ })` but commit `b390638` flipped the canvas-host to `role="tree"`. three specs still failing on trunk: `family-view-add-relative.spec.ts`, `family-view-continuity.spec.ts`, `collapse-badge-end-to-end.spec.ts`. fix: replaced `getByRole("region", ...)` with `getByRole("tree", ...)` across all three files (4 call-sites total). 🎯 carried in from plan: canvas-window-manager (2026-05-27).
- :red_circle: `27 May 2026` arrow-key pan directions inverted · closed in ffb73b3 · sign flip on pan deltas in TreeCanvas.svelte and FamilyViewCanvas.svelte so arrow direction matches viewport-motion direction
- :red_circle: `27 May 2026` family-view debug coi-breakdown panel did not register when the focus had zero consanguinity (issue 1 of canvas-window-manager). registration gate at `FamilyViewDebugOverlay.svelte:1320` ANDed `coiBreakdown.length > 0`, hiding the dock entry entirely when the focus produced no rows. fix dropped the length clause; the body handles zero rows gracefully (`—` values + empty tbody). e2e in `family-view-debug-coi.spec.ts` pins the empty-tree case. 🎯 shipped 2026-05-27 (canvas-window-manager plan; see git history).
- :red_circle: `27 May 2026` floating canvas surfaces invented their own chrome — different padding, no shared titlebar, no close affordance, no minimize-into-pill (issues 3 / 4 / 10 / 12 of canvas-window-manager). the canvas-window-manager plan introduced a single `Window` primitive (5-prop contract: pillId / title / expanded / forcedCollapse / onToggleExpanded / body) that every floating surface routes through. titlebar is always `title [chev-down] [chev-right/left] [×]`; bodies are snippets; pop-out re-docks back to the same pill. debug menu / stats popover / save-status popover / 5 family-view debug panels all migrated; `CanvasChromePill` deleted. 🎯 shipped 2026-05-27 (canvas-window-manager plan; see git history).
- :red_circle: `27 May 2026` floating windows had no z-order / focus-to-front / pop-out semantics — windows piled on top of each other and could not be dragged aside to reference (issues 9 / 12 of canvas-window-manager). `windowManager.svelte.ts` runtime added: focus + popOut + redock + bringToFront + moveTo with bbox clamp; `WindowOverlay.svelte` paints popped-out windows in canvas-host coordinates at z-30..z-49; dockRegistry's `itemsForCorner` sort gained a `focusedAt` tiebreaker scoped to `kind="window"` so focus-to-front works without disturbing pill order. popped-out wrappers do NOT carry `data-canvas-chrome` (phase-0 probe verdict) so `fitToView` pans content under them. 🎯 shipped 2026-05-27 (canvas-window-manager plan; see git history).
- :red_circle: `27 May 2026` debug pill, debug menu, and debug effects were conflated under one `debugOpen` flag — closing the menu cleared every overlay (issue 7 of canvas-window-manager). phase 3 split the master switch (`debugMode`, persisted in `fte.debug.mode`) from the menu's transient open state (`debugMenuOpen`). help menu adds a Debug mode toggle; menu body adds a "disable debug mode" button distinct from the titlebar ×. closing the menu via × leaves overlays running; only an explicit master-switch flip retires them. agents.md §18 documents the split. 🎯 shipped 2026-05-27 (canvas-window-manager plan; see git history).
- :red_circle: `27 May 2026` save-status pill collapsed local persistence and remote sync into one indicator (issues 5 / 6 of canvas-window-manager). phase 4: the pill renders two lucide glyphs inline — local (`laptop-minimal` / `laptop-minimal-check`) + remote (`cloud` / `cloud-check` / `cloud-off` / `cloud-upload` / alert variants) — and the Window body splits into three rows (local / remote / runtime, the last gated by `debugMode`). 🎯 shipped 2026-05-27 (canvas-window-manager plan; see git history).
- :red_circle: `27 May 2026` stats pill displayed people-count regardless of which metric the user wanted to track; standalone `editRev` pill cluttered the always-visible chrome (issues 2 / 8 of canvas-window-manager). phase 4: stats Window rows become buttons that write to `selectedMetric: $state<"people" | "clusters" | "descendants" | "coi">`; the stats pill branches on it. `rev` removes from the stats Window and relocates to the save-status Window's debug-only runtime row alongside `debugTimings.total`. the standalone debug-timings pill (was priority 40 in the bl dock) deletes — no data dropped. 🎯 shipped 2026-05-27 (canvas-window-manager plan; see git history).
- :red_circle: `27 May 2026` debug-menu open latency was high enough to be noticeable on cramped viewports (issue 11 of canvas-window-manager). phase-0 root-cause analysis named "load-bearing identity churn" — DockRegistration's second `$effect` allocated a fresh `next` object literal on every reactive churn and called `updateItem` regardless, fanning derives across every dock consumer. phase 4 fix: field-by-field identity check against the prior write; effect early-returns when nothing meaningful changed. benchmark assertion in code (`canvas-window-manager.spec.ts`): open-menu p50 < 200ms on chromium. 🎯 shipped 2026-05-27 (canvas-window-manager plan; see git history).
- :red_circle: `23 May 2026` PersonNode portrait img already uses `object-cover object-top` (apps/web/src/lib/components/tree/PersonNode.svelte:175); square 600x600 source crops cleanly into the 3:4 slot, no stretch path remains.
- :red_circle: `26 May 2026` debug menu migrated into the `CanvasChromeDock` - registered as `id: "debug-menu"`, `corner: "bl"`, `priority: 300`, `kind: "panel"`, `forceCollapsible: false` so the dock never force-collapses the primary control surface; menu height clamps to `min(80vh, calc(100vh - var(--inspector-sheet-height, 0px) - 16rem))` so Pixel 7 + sheet inspector stays inside the dock cap and one debug panel still fits on cramped 1280×720. css-var bridge (`--debug-menu-bottom` / `--debug-menu-height` arms of the dock's `bottom:` calc, the `$effect` in App.svelte that wrote them, and the `debugMenuEl` ref) deleted. planned in [.claude/plans/archived/migrate-debug-menu-into-dock/](../.claude/plans/archived/migrate-debug-menu-into-dock/).

### shell

- :red_circle: `28 May 2026` SaveStatusPill popover "last saved" line read "never" on a freshly loaded tree - `lastSavedAt` was session-scoped (only set when the autosave path ran in the current tab), so a reload-then-look-at-pill flow always rendered "never" (or the ambiguous "saved" without a relative timestamp). fix: `loadTree` in `persistence/trees.ts` now returns `{ tree, savedAt }` (`LoadedTree`) instead of the bare `Tree`; both call sites in `App.svelte` (`onMount` initial load + `loadFromRecents`) assign `lastSavedAt = r.value.savedAt` immediately after hydrate, seeding the popover with the dexie row's `updatedAt` timestamp. `OpenDialog.svelte` preview path updated to use `r.value.tree`. test coverage added in `tests/unit/persistence/dexie.test.ts`.
- :red_circle: `27 May 2026` Home key did not auto-centre on the root person - the `Home` combo was declared in `shortcuts.ts` from the first commit but originally resolved to `stub("Center on root")`. the 23 May 2026 refactor to the `handlers`/`buildCommands` pipeline wired `view.centerRoot` to the real `viewCenterRoot` handler (`withCanvas(c => c.centerOnRoot())` + `selection.select(rootId)`). Home now dispatches the same action as Tree > "center on root".
- :red_circle: `27 May 2026` auth dry-run sign-out did not clear `authDryRunEnabled` or `localStorage["fte.debug.authDryRun"]` - `AuthBar.signOut()` called `authStore.setDryRun(false)` directly, bypassing the `authDryRunEnabled` state in `App.svelte` and the `localStorage` write. fix: added `ondryrunchange` prop to `AuthBar`; parent now owns the state transition, and the existing `$effect` that calls `setDryRun` fires naturally. `App.svelte` + `AuthBar.svelte`; closed in `671cdda`.
- :red_circle: `26 May 2026` command-palette / find-person pick closed the palette but the canvas re-centred on the previous selection - the 14 May fix called `canvasController?.focusSelection()` after `focusPerson(id)`, but `focusSelection` reads the canvas's own `selectedId` prop, which hasn't yet propagated from the just-written selection store on the same synchronous tick. result: the canvas centred on the OLD selectedId while the inspector + selection store correctly pointed at the new id, giving the impression that "the pick didn't take." fix in `App.svelte` `onPalettePick`: swap `focusSelection()` for `centerOnPerson(id)`, which takes the new id explicitly and bypasses the stale-prop race.
- :red_circle: `14 May 2026` command-palette pick left canvas in place - `App.svelte:1156` now calls `canvasController?.focusSelection()` after `focusPerson`; all three engines recenter on the picked person.

### io / parsers

- :red_circle: `27 May 2026` 3 `parseFamilyEchoHtml` test failures due to dead symlink - `apps/web/tests/fixtures/familyecho-sample.html` pointed at the non-existent `My-Family-24-May-2026-103040590.html`; retargeted to the existing `My-Family-24-Apr-2026-233811725.html` and updated portrait-related assertions to match. closed in 6a458b0.

---

## meta

### format

mirrors [to-do.md](to-do.md) so the same `commit-style` / triage habits apply.

open bug: `- :o: \`priority\` \`effort\` short summary - repro / context / suspected root cause / suggested fix`

fixed bug: `- :red_circle: \`9 May 2026\` short summary - one-line note on the fix`

cross-reference to a plan file: append `:dart: *planned in [<file>.md](<path>)*` to the item, or hoist it onto a section header line if every item in the section shares the same plan. keep it terse - no inline rationale.

priority levels (highest to lowest): `high priority`, `medium priority`, `low priority`, `future idea`

effort levels: `no effort`, `low effort`, `medium effort`, `high effort`, `very high effort`

### scope

a "bug" here is a defect with observable wrong behavior on a working build - a wrong line on the canvas, a focused field that swallows input, a stuck cursor. **not** a bug:

- missing functionality that was never wired up (e.g. arrow-key canvas pan, keyboard tree navigation) - those are features in `to-do.md`
- known architectural limitations the team has chosen not to address yet (e.g. last-write-wins autosave conflict resolution) - features
- polish or quality bumps without a wrong-output repro (e.g. zoom-aware label sizing, edge thickness at far zoom) - features
- deployment / infra plumbing - features

if a feature in `to-do.md` turns up a defect during implementation, file the defect here and link from the feature.

### sections

items are grouped first by view if specific to one (family-view, tree-view, hyperbolic), then by component (inspector, canvas, shell, portrait cropper, etc.). within each section, open items sort by priority (high first); fixed items sort by date (newest first).

- **open** - active defects
- **fixed** - resolved defects kept for reference; pruned when no longer informative
- **meta** - this section; describes the doc format

### metadata

```yaml
last_updated: 30 Jun 2026
total_fixed: 51
notes: recomputeAfterCollapse+expandedSecondaryUnions fix (28 May 2026) absorbed into the adjacent-finding note on the "also show alongside" open entry; no standalone fixed entry added; 4 wiki-editor entries migrated in from wiki-editor-syntax and wiki-editor-theme plans during pre-merge (30 Jun 2026)
```
